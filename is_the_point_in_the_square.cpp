#include <iostream>
using namespace std ;
int main ( )
{float X,Y;
cout<<" X = " ; cin>>X;
cout<<" Y = " ; cin>>Y;
if (X>=-1 && X<=3 && Y>=-2 && Y<=4)
cout <<"the point lies in the square"<< endl ;
else
cout<<"the point doesn't' lie in the square"<<endl ;
return 0;
}
